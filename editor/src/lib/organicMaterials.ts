/** Studio export compatibility surface; material creation is shared with the public renderer. */
export {
  createMaterialForPreset,
  exportMaterialParams,
  type ExportMaterialParams,
} from '@namche/metaball-react';
