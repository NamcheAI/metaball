/** Studio compatibility surface; the canonical material catalog lives in the public renderer. */
export {
  MATERIAL_PRESETS,
  getMaterialPreset,
  type MaterialPreset,
  type MaterialPresetId,
} from '@namche/metaball-react';
